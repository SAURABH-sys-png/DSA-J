#include <bits/stdc++.h>

using namespace std;



void solve(int t) {
    cout << pow(2, t) - 1 << endl;
    
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(NULL);

    int t = 1;
    cin >> t;
    solve(t);

    return 0;
}