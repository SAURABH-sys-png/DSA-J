#include <bits/stdc++.h>
#define ll long long
#define fastio               \
    ios::sync_with_stdio(0); \
    cin.tie(0);              \
    cout.tie(NULL);
using namespace std;

void solve()
{
    int n;
    cin >> n;
    
    vector<ll> arr(n + 1, 0);
    for (int i = 1; i < n; i++)
    {
        cin >> arr[i];
    }

    vector<int> dpt(n + 1,0);
    
    


}

int main()
{
    int t = 1;
    cin >> t;
    while (t--)
    {
        solve();
    }

    return 0;
}