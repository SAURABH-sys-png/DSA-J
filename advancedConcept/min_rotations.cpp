#include <bits/stdc++.h>
#define ll long long
#define fastio ios::sync_with_stdio(0); cin.tie(0); cout.tie(NULL);
using namespace std;



ll solve(ll input, ll target){
    if(input == target)return 0;
    
    
}

int main() {
    fastio
    ll input,target;
    cin >> input >> target;

    ll ans = solve(input,target);
    cout << ans << endl;
    return 0;
}